---
name: "pl-deploy-server"
display-name: "PL Deploy Server"
description: "Deploy Dataverse Label Translator server components through the DevKit Server project batch file."
---

# pl-deploy-server

Use this skill when the user asks to run the Dataverse Label Translator command `pl-deploy-server`.

## Command Template

# Deploy Server

When server code is edited under `DataverseLabelTranslator.Server`, run `DataverseLabelTranslator.Server\deploy.debug.bat` from the repo root and report only the DevKit summary or error.
